# v2.1.0 最终逻辑

1. 首页卡片仍只展示用户选择的 0～3 项明确延迟任务。
2. 点击卡片延迟区域后，详情展示当前节点被分配的全部延迟任务；不再使用鼠标悬停触发，避免经过相邻卡片时误触。
3. 详情折线表示延迟，彩色竖条表示对应任务在具体时刻的丢包或断连；Tooltip 同时显示 ms 与丢包百分比。
4. 点击遮罩、关闭按钮或按 Esc 可关闭详情。
5. 首页底部显示当前访问者公网 IP，可在主题设置中关闭或替换查询接口。
6. 一个展示项始终对应一个任务，不进行跨城市、跨线路平均。
