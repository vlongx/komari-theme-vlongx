# GitHub 上传说明

源码目录执行：

```bash
git add .
git commit -m "feat: add click latency details, loss bars and visitor IP"
git push
```

创建 `v2.1.0` Release，并上传：

```text
komari-theme-tasogare-latency-2.1.0-komari.zip
```

Komari 后台需要上传 Release 附件中的安装包，不要上传 GitHub 自动生成的 Source code ZIP。
