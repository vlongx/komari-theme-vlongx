# Changelog

## 2.2.3

- 基于 v2.2.2 的 TCPing Footer Spacing 继续扩展节点卡片底部信息区。
- 在实时上下行网速与在线时长之间新增服务器费用显示。
- 费用读取 Komari 节点的 `price`、`currency` 与 `billing_cycle`，自动显示对应币种与周期。
- 未配置价格的节点自动隐藏费用，保持原有底部布局。
- 保留 v2.2.2 的 TCPing 安全间距、窄屏和高 DPI 布局修复。

## 2.2.2

- 修复第三条 TCPing 历史点阵紧贴底部状态栏分隔线的问题。
- 在 TCPing 区域末尾增加固定安全间距，确保最后一行点阵完整显示。
- 固定底部网速/在线时长状态栏的伸缩行为，避免窄屏布局将其向上挤压。

## 2.2.1

- 修复窄屏和高 DPI 环境下三条 TCPing 任务与卡片底部信息挤在一起的问题。
- 将实时网速、在线时长和标签整理为独立卡片底部区域，并增加分隔线。
- 为延迟/丢包双列补充最小宽度和移动端间距约束，避免数值或状态条溢出。
- 节点卡片改用可键盘操作的语义容器，避免卡片内嵌交互控件造成的浏览器布局差异。

## 2.2.0

- 基于 Tasogare v1.2.2 整合本项目现有自选延迟功能。
- 服务器详情中的 CPU 型号改为完整显示，不再截断。
- 新增 GPU / iGPU 硬件详情，支持独显、Intel/AMD 核显与多 GPU 数据。
- GPU 名称进行精简显示，例如 `Intel Corporation CometLake-S GT2 [UHD Graphics 630]` 显示为 `Intel UHD Graphics 630`。
- 过滤 Sensor Hub 等非显卡设备，避免将芯片组传感器误识别为 GPU。
- GPU 节点显示当前使用率与显存/共享内存；无 GPU 节点自动隐藏 GPU 项目。
- 从服务器详情面板移除延迟图表；首页点击延迟区域打开的独立延迟/丢包弹窗保持不变。
- 新增按月流量周期计算，避免 Agent 累计流量长期导致卡片显示 100%。
- 主题设置新增 `trafficResetDay`（1～28）作为全局流量重置日。
- 支持节点标签 `traffic-reset:13` 等形式覆盖单台服务器的重置日。
- 流量周期计算兼容累计计数器回零/重启场景。
- 保留 TCP/UDP 连接数、访问 IP、剩余价值、自选延迟任务与丢包状态条等现有功能。

## 2.1.0

- 延迟详情由鼠标悬停触发改为点击触发，避免移动到相邻节点时误弹窗。
- 点击卡片延迟区域打开详情；点击遮罩、关闭按钮或按 Esc 关闭。
- 延迟图新增丢包竖条：折线表示延迟，彩色竖条表示对应任务在该时刻的丢包或断连。
- 图表 Tooltip 同时区分延迟（ms）与丢包（%）。
- 首页底部新增访问 IP 信息，支持后台开关和自定义查询接口。
- 项目地址更新为 vlongx/komari-theme-vlongx。

## 2.0.0

- 首页卡片继续只展示用户自由选择的 0～3 个延迟任务。
- 鼠标悬停详情改为展示当前节点被分配的**全部延迟监测任务**，不受首页选择限制。
- 悬浮详情按需请求全部任务历史，关闭时不加载，减少首页初始开销。
- 首页任务名称增加紧凑运营商缩写：联通 CU、电信 CT、移动 CM，并保留地区前缀。
- 每个首页任务增加 TCP、ICMP 或 HTTP 协议徽标，混合选择时也能直接区分。
- 丢包列取消重复任务名称，仅保留丢包值和历史状态条。
- 悬浮标题去除重复“延迟”文字，说明改为“显示该节点全部延迟监测任务”。
- 悬浮摘要卡和图例支持更多任务并自动换行。

## 1.9.0

- 首页延迟展示由固定三网识别改为读取 Komari 真实延迟监测任务。
- 新增动态任务选择器，用户可自由选择 0～3 项任务并调整顺序。
- 支持 TCP、ICMP、HTTP 任意混合组合。
- 每个展示项严格对应一个任务，不再跨城市或跨任务求平均。
- 支持自定义显示名称，例如“广东联通 TCP”可显示为“联通”。
- 选择结果保存在当前浏览器；主题设置可提供全站默认任务和名称。
- 仅展示实际分配给当前节点的任务；无适用任务时自动隐藏模块。
- 悬浮延迟大图同步只展示当前选择的任务。
- 保留 TCP/UDP 连接数、剩余价值、丢包状态条和 Tasogare 原有界面。

## 1.8.0

- 支持按 TCPing、ICMP、HTTP 或全部任务类型切换主页延迟。

## 1.7.0

- 增加延迟悬浮大图与时间范围切换。
